import { VisibleInMouseDirective } from './visible-in-mouse.directive';

describe('VisibleInMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleInMouseDirective();
    expect(directive).toBeTruthy();
  });
});
